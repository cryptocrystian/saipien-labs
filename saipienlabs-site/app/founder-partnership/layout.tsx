import { founderPartnershipMetadata } from '@/lib/seo/founderPartnershipSEO';

export const metadata = founderPartnershipMetadata;

export default function FounderPartnershipLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
